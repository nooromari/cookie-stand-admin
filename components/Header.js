import styles from '../styles/Home.module.css'


function Header(){
    return(
        <header>
            <h1 className={styles.title}>
                Cookie Stand Admin
            </h1>

        </header>
    )
}

export default Header